# React + TypeScript + Vite + shadcn/ui

Ottr FluidGlass demo — Vite, React, TypeScript, and shadcn/ui with React Bits FluidGlass.

## Adding components

```bash
npx shadcn@latest add button
```

## Scripts

```bash
pnpm install
pnpm dev
pnpm typecheck
pnpm build
```
